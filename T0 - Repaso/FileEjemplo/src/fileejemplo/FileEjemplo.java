/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package fileejemplo;

import java.io.*;

/**
 *
 * @author Usuario DAM 2
 */
public class FileEjemplo {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) throws IOException {
        /*File fichero = new File("src/documentos/multimedia.obj");
        FileOutputStream ficheroOutput = null;
        ObjectOutputStream flujoObjetoSalida = null;
        
        
        try {
            ficheroOutput = new FileOutputStream(fichero);
            flujoObjetoSalida = new ObjectOutputStream(ficheroOutput);
        } catch (FileNotFoundException e) {
            e.printStackTrace();

        } catch (IOException e) {
            e.printStackTrace();
            System.out.println(" Error producido en el proceso de salida");
        } finally {
            try{
                flujoObjetoSalida.close()
            } catch {
                
            }
            ;
        }*/
        
        
        //ListaHastable = (HastTable<String, Alumno>) flujoLecturaObjetos.readObject();
        //Enumeration<Alumno> elementos = ListaHastable.elements();
        //while(elementos.hasMoreElements()){
    //      Alumno encontrado = elementos.nextElement();
    //      System.out,println(encontrado);
    //}

    }

}
